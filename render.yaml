services:
  - type: web
    name: colinas-verdes
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: DEPLOY_STAMP
        value: "6fd9c6d"
      - key: NODE_VERSION
        value: 22
