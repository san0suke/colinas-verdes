services:
  - type: web
    name: colinas-verdes
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: DEPLOY_STAMP
        value: "f8b73b2"
      - key: NODE_VERSION
        value: 22
